function saveCircleTIF(I, centroid, r, color)
% LABELPARTICLES save a stack of TIFs for verifying circle detection.
% 
% saveCircleTIF(I, centroid, r) saves a bunch of tifs in the directory 'circled Spots'. I is an image stack where the 

for n = 1:length(I)

end